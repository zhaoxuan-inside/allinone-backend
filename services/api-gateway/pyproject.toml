[tool.poetry]
name = "api-gateway"
version = "0.1.0"
description = "API Gateway for AllInOne Platform"
authors = ["Developer <developer@example.com>"]

[tool.poetry.dependencies]
python = "^3.13"
fastapi = "^0.115.0"
uvicorn = "^0.32.0"
httpx = "^0.27.0"
pydantic = "^2.7.0"
nacos-sdk-python = "^0.3.0"
aiohttp = "^3.9.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"